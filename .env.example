VITE_API_BASE_URL=http://localhost:5080
VITE_ANPR_SERVICE_URL=http://localhost:8001
VITE_FACE_SERVICE_URL=http://localhost:8002
VITE_BEHAVIOR_SERVICE_URL=http://localhost:8003

# Style bản đồ nền tự host (xem ../map-tile-server) — để trống thì fallback về
# tile OpenStreetMap công cộng (chỉ nên dùng cho dev local, KHÔNG dùng khi
# triển khai thật vì sẽ gọi ra ngoài mỗi lần pan/zoom bản đồ).
VITE_MAP_STYLE_URL=http://localhost:8091/style.json
